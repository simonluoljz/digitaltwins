server:
  port: 8002
spring:
  application:
    name: digitaltwins_knowledgegraph
  cloud:
    config:
      discovery:
        enabled: true
        service-id: digitaltwins-config
      profile: dev
# eureka 配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://root:123@localhost:8761/eureka
env: 
  dev


